name: messos
description: "MessOS — hostel mess subscriptions, QR meal attendance and counter operations."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.0-228.0.dev

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.4.3
  go_router: ^18.0.1
  dio: ^5.11.1
  flutter_secure_storage: ^10.3.1
  intl: ^0.20.3
  qr_flutter: ^4.1.0
  screen_brightness: ^2.1.11
  supabase_flutter: ^2.17.2
  shared_preferences: ^2.5.5
  mobile_scanner: ^7.4.0
  audioplayers: ^6.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/brand/mark.png

  # Bundled rather than fetched at runtime. The `google_fonts` package downloads
  # on first use, which costs a cold start exactly where we just removed one and
  # leaves the app looking wrong offline — at a mess counter, offline is normal.
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
          weight: 400
        - asset: assets/fonts/Inter-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Inter-Bold.ttf
          weight: 700
        - asset: assets/fonts/Inter-ExtraBold.ttf
          weight: 800

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Generated with `dart run flutter_launcher_icons`. The sources live in
# assets/brand/ and are built by the script in the same commit — regenerate
# rather than hand-editing anything under android/app/src/main/res or
# ios/Runner/Assets.xcassets.
flutter_launcher_icons:
  image_path: "assets/brand/icon.png"
  # iOS forbids alpha in an app icon and composites transparency onto black,
  # which would ring a white-bodied mark in dark grey.
  remove_alpha_ios: true
  background_color_ios: "#FFFFFF"
  ios: true
  android: true
  # The launcher crops an adaptive icon to a circle or squircle, so the
  # foreground is inset and white supplies the ground behind it.
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/brand/icon_foreground.png"

# Generated with `dart run flutter_native_splash:create`. The native launch
# screen is what shows *before* Flutter starts — without it the app opens on a
# blank white rectangle and then jumps to a branded screen, which is most of why
# a cold start feels cheap.
#
# Colours match the app's own surfaces so there is no visible seam between the
# native screen and the first Flutter frame.
flutter_native_splash:
  color: "#FAFAFD"
  color_dark: "#121317"
  image: assets/brand/splash.png
  image_dark: assets/brand/splash.png

  android_12:
    # Android 12+ draws the icon inside a circle and crops anything outside it,
    # so this variant is inset further than the one older versions use.
    image: assets/brand/splash_android12.png
    image_dark: assets/brand/splash_android12.png
    color: "#FAFAFD"
    color_dark: "#121317"

  android: true
  ios: true
  web: false
